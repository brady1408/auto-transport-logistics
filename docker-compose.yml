services:
  postgres:
    image: postgres:16
    container_name: atlinks-pg
    environment:
      POSTGRES_DB: atlinks
      POSTGRES_USER: atlinks
      POSTGRES_PASSWORD: atlinks_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
